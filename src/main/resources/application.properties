# Server Port
server.port=8082

# Database Configuration for PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/java_dev_api
spring.datasource.username=kaitest
spring.datasource.password=Kv1ponder!

# spring.datasource.password=${DB_PASSWORD}

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


spring.datasource.hikari.maximum-pool-size=10

# Spring Session Variables
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always
spring.session.timeout.seconds=600

# H2 Console Configuration (Optional)
spring.h2.console.enabled=true
